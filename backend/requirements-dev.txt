-r requirements.txt
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
# Chặn trên: bản minor mới của ruff đổi bộ rule mặc định (0.15 → 0.16 làm CI
# đỏ 70 lỗi trên code không đổi). ruff.toml đã chốt select nên rủi ro nhỏ đi
# nhiều, nhưng vẫn chặn để nâng bản là một quyết định có chủ đích.
ruff>=0.16,<0.17
